{
  "tasks": {
    "dev": "ENV=development deno run --watch -A src/main.ts",
    "check": "deno fmt --check && deno lint",
    "install-hooks": "deno run -A -r https://deno.land/x/githooks/githooks.ts"
  },
  "githooks": {
    "pre-commit": "check"
  }
}
